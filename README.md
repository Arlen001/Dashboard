Made a dashboard using flask
To run this webpage in your own enviroment need the following:
        install python3
        install pip3
        pip install flask
        pip install flask_pymongo
        
Start application:
        python3 app.js
